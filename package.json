{
  "name": "@gov.nasa.jpl.stellar/stellar",
  "version": "1.0.21",
  "description": "A CSS, HTML, and JavaScript implementation of the Stellar design system.",
  "license": "UNLICENSED",
  "repository": {
    "type": "git",
    "url": "https://github.jpl.nasa.gov/Stellar/stellar.git"
  },
  "scripts": {
    "build": "rm -rf css && sass --no-source-map scss:css",
    "lint": "stylelint scss/**/*.scss --fix"
  },
  "devDependencies": {
    "sass": "^1.43.4",
    "stylelint": "^14.0.1",
    "stylelint-config-standard-scss": "^2.0.1",
    "stylelint-order": "^5.0.0"
  }
}
